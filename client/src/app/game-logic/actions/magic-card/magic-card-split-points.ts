import { MagicCard } from '@app/game-logic/actions/magic-card/magic-card';

export class SplitPoints extends MagicCard {
    protected perform() {
        // Don't care for offline game, we need to move it to a commun folder
    }
}
