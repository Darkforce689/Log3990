export interface GameSettings {
    timePerTurn: number;
    playerName: string;
    botDifficulty: string;
    privateGame: boolean;
    randomBonus: boolean;
    dictTitle: string;
    dictDesc?: string;
}
