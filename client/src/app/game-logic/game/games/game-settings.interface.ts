export interface GameSettings {
    timePerTurn: number;
    playerName: string;
    botDifficulty: string;
    privateGame: boolean;
    gameStatus: string;
    randomBonus: boolean;
    magicCardIds: string[];
}
